---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import Logo from '../components/Logo.astro';
---

<BaseLayout title="SwiftClick Estructura - SwiftClick">
	<section class="min-h-screen relative z-10 overflow-hidden px-4 sm:px-6 md:px-8 lg:px-16 flex flex-col justify-center" style="width: 100vw; min-height: 100vh; background: linear-gradient(to bottom right, #0a0a0f 0%, #13131f 50%, #1a1a2e 100%);">
		
		<!-- Luz violeta arriba a la derecha -->
		<div class="absolute top-0 right-0 w-1/2 sm:w-1/3 h-1/3 sm:h-1/2 pointer-events-none" style="background: radial-gradient(ellipse at top right, rgba(157, 141, 241, 0.2) 0%, rgba(106, 90, 205, 0.1) 30%, transparent 70%); filter: blur(80px) sm:blur(100px);"></div>
		
		<!-- Logo en la esquina superior izquierda -->
		<div class="absolute top-4 sm:top-6 md:top-8 left-3 sm:left-4 md:left-6 lg:left-8 xl:left-16 z-20">
			<Logo variant="default" size="md" showText={true} />
		</div>

		<!-- Contenedor principal centrado -->
		<div class="max-w-5xl mx-auto w-full z-10 text-center px-2">
			
			<!-- Texto principal -->
			<h1 class="text-base sm:text-lg md:text-xl lg:text-2xl xl:text-3xl 2xl:text-4xl font-medium text-white leading-relaxed">
				<span style="color: #b8a9ff;">SWIFTCLICK</span> ES UNA ESTRUCTURA PENSADA<br class="hidden sm:block">
				PARA QUE LOS ANUNCIANTES GRANDES<br class="hidden sm:block">
				PUEDAN MANEJAR SU INVERSIÓN DIGITAL SIN<br class="hidden sm:block">
				CONTRATIEMPOS: CON SOPORTE CERCANO Y<br class="hidden sm:block">
				UNA OPERACIÓN QUE LES DA <span style="color: #b8a9ff;">MÁS CONTROL</span> Y<br class="hidden sm:block">
				<span style="color: #b8a9ff;">MEJOR RENDIMIENTO</span>, SIN COMPLICACIONES<br class="hidden sm:block">
				INNECESARIAS.
			</h1>

		</div>

		<!-- Número 10, texto y línea en el bottom -->
		<div class="absolute left-0 right-0" style="bottom: 16px; sm:bottom:20px; md:bottom:24px;">
			<div class="px-4 sm:px-6 md:px-8 lg:px-16">
				<!-- Texto inferior centrado arriba de la línea -->
				<div style="margin-bottom: 40px; sm:margin-bottom:50px; md:margin-bottom:60px; transform: translateY(20px); sm:transform:translateY(25px); md:transform:translateY(30px);">
					<p class="text-xs sm:text-sm md:text-base lg:text-lg text-white/70 text-center px-2" style="font-weight: 200;">
						Quedamos a disposición para integrarnos según las necesidades de cada equipo o agencia.
					</p>
				</div>
				
				<div class="flex justify-end items-end mb-2">
					<span class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold leading-none" style="color: #b8a9ff; text-shadow: 0 0 40px rgba(184, 169, 255, 0.3);">
						10
					</span>
				</div>
				<div class="w-full h-0.5" style="background: linear-gradient(to right, transparent 0%, #b8a9ff 50%, transparent 100%);"></div>
			</div>
		</div>

		<Navigation currentPage={10} totalPages={11} />
	</section>
</BaseLayout>
