---
import { ViewTransitions } from 'astro:transitions';
import '../styles/global.css';

interface Props {
	title?: string;
}

const { title = 'SwiftClick S.A. - Marketing Digital' } = Astro.props;
---

<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes" />
		<meta name="generator" content={Astro.generator} />
		<meta name="theme-color" content="#1a1a2e" />
		<meta name="description" content="SwiftClick S.A. - Partner Estratégico en Inversión Digital a Gran Escala. Google Partner, Multicanal, Performance & Escalabilidad." />
		<title>{title}</title>
		<!-- Tipografías según Manual de Marca SwiftClick -->
		<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
		<link href="https://fonts.cdnfonts.com/css/tt-commons" rel="stylesheet">
		<ViewTransitions />
		<style is:global>
			@keyframes slideFromRight {
				from {
					opacity: 0;
					transform: translateX(100%) scale(0.95) rotateY(-15deg);
				}
				to {
					opacity: 1;
					transform: translateX(0) scale(1) rotateY(0deg);
				}
			}

			@keyframes slideFromLeft {
				from {
					opacity: 0;
					transform: translateX(-100%) scale(0.95) rotateY(15deg);
				}
				to {
					opacity: 1;
					transform: translateX(0) scale(1) rotateY(0deg);
				}
			}

			@keyframes slideToLeft {
				from {
					opacity: 1;
					transform: translateX(0) scale(1) rotateY(0deg);
				}
				to {
					opacity: 0;
					transform: translateX(-30%) scale(0.9) rotateY(15deg);
				}
			}

			@keyframes slideToRight {
				from {
					opacity: 1;
					transform: translateX(0) scale(1) rotateY(0deg);
				}
				to {
					opacity: 0;
					transform: translateX(30%) scale(0.9) rotateY(-15deg);
				}
			}

			::view-transition-old(root) {
				animation: slideToLeft 0.8s cubic-bezier(0.85, 0, 0.15, 1);
			}

			::view-transition-new(root) {
				animation: slideFromRight 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
			}

			html[data-direction="back"]::view-transition-old(root) {
				animation: slideToRight 0.8s cubic-bezier(0.85, 0, 0.15, 1);
			}

			html[data-direction="back"]::view-transition-new(root) {
				animation: slideFromLeft 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
			}
		</style>
	</head>
	<body class="bg-gray-900 text-white overflow-hidden" style="font-family: 'TT Commons', 'Roboto', sans-serif;">
		<slot />
	</body>
</html>
