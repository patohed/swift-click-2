---
interface Props {
	variant?: 'default' | 'light' | 'dark';
	size?: 'sm' | 'md' | 'lg';
	showText?: boolean;
}

const { variant = 'default', size = 'md', showText = true } = Astro.props;

const sizeClasses = {
	sm: 'w-6 h-6 sm:w-10 sm:h-10',
	md: 'w-10 h-10 sm:w-16 sm:h-16',
	lg: 'w-12 h-12 sm:w-20 sm:h-20'
};

const textSizeClasses = {
	sm: 'text-base sm:text-2xl',
	md: 'text-lg sm:text-3xl',
	lg: 'text-xl sm:text-4xl'
};
---

<div class="flex items-center space-x-2 sm:space-x-3">
	<!-- SwiftClick Logo SVG -->
	<div class="relative">
		<img 
			src="/swift_click.svg" 
			alt="SwiftClick Logo" 
			class={sizeClasses[size]}
		/>
	</div>
	
	{showText && (
		<div>
			<div class={`${textSizeClasses[size]} font-bold tracking-tight`}>
				<span class="text-white">Swift</span><span class="text-blue-400">Click</span>
			</div>
		</div>
	)}
</div>
